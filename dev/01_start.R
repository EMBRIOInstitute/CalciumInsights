# Project initialization record.
# The golem package structure is already initialized.
# Dependencies are declared in DESCRIPTION.
# Run source("install_dependencies.R") from the repository root,
# then source("dev/run_dev.R").
